# pulseaudio settings
export PULSE_SERVER="tcp:audioserver"
export PULSE_SINK="usb_dddac"

# when using X
if [ -z "${DISPLAY}" ]; then
  export DISPLAY=":0.0"
fi

# xbmc audio engine {SOFT,PULSE,ACTIVE}
export AE_ENGINE=ACTIVE
